<%= form_for(@operatingsystem) do |f| %>
  <% if @operatingsystem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operatingsystem.errors.count, "error") %> prohibited this operatingsystem from being saved:</h2>

      <ul>
      <% @operatingsystem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
